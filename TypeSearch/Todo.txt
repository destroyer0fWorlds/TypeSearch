- add string methods to other types to support fuzzy searching
    - add tests
        - guid
        - guid?
- add not strongly typed tests for interacting with the where criteria directly
- add support for the only types that i am not doing anything with:
	- Char
	- TimeSpan
- add support for navigational properties. as in:
    - var x = new x();
    - var z = x.y.z